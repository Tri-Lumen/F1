/**
 * Shared layout constants. The sidebar width is also exposed as the
 * `--sidebar-w` CSS variable in globals.css; keep the two in sync so the fixed
 * sidebar and the content offset can never drift apart.
 */
export const SIDEBAR_WIDTH = 224;

/** Max width of the centred content column on very wide / Electron windows. */
export const CONTENT_MAX_WIDTH = 1600;
